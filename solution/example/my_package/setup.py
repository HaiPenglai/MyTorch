from setuptools import setup

# TODO修改下面的代码，定义一个名为my_package的包，版本号为0.1，使得可以用pip install my_package安装该包，可用import packageA导入packageA这个包

'''《
setup(
    name='?',
    version='?',
    packages=['?']
)
》'''

#《
setup(
    name='my_package',
    version='0.1',
    packages=['packageA']
)
#》